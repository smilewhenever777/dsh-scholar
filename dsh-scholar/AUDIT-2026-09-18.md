# dsh-scholar 代码审查 2026-09-18（第三轮，v1.17–v1.24 增量全审）

范围：v1.16 审计后的全部新代码——引用格式（citation.ts）、精读任务系统
（v1.18 加入、v1.21 拆除）、自有精读引擎（src/read/ 八模块）、13 工具、
读相关路由、PaperAsk/PaperReports/DeepreadLauncher/SettingsSection 客户端。

## 发现与处置（全修）

**A1｜P0 后台路径丢图**：engine.read 的后台 job 分支调用 runFlow 后未设置
`meta.figuresImgs`（只有 readSyncInner 与前台尾部设置）→ paper_read 工具的
长文后台任务归档的报告**没有嵌入图**。修：job 内 runFlow 后补设。

**A2｜死代码：任务系统残留**：store.ts 的 task 四方法 + readDeepreadTaskFile
+ DeepreadTask 类型 + readFileSync 导入全部移除（v1.21 拆任务时漏删）。

**A3｜死 locale 键**：deepread.trigger/existing/footHint(一代)/map 中英 8 键
移除（paper.deepreadSent/Filled/Copied 保留——建卡指令仍在用）。

**A4｜过时注释**：DeepreadLauncher 顶部仍描述"POST /scholar/deepread/task"
旧流程，更新为 read/run 直跑。

**A5｜figures.ts 流尾字节**：endstream 前只减一个 \n，\r\n 时残留 \r 混入
图像字节头。修：连续减 \r 与 \n。

**A6｜vlm 附件名后缀**：jpeg 图也命名 .png，按 mime 给 .jpg/.png。

**复核通过项（无改动）**：
- render.ts XSS 面：模型输出全部经 esc()，b64 仅进 src 属性（字符集安全）
- systemPrompt 无已删工具残留；routes 全部 guardRoute（loopback+token）
- ask.ts 检索 regex 转义正确（实测含 Table N 问题）；session 原子写 ✓
- paper_read onResult 闭包的 store 引用：目录热切换后 upsertPaper 显式报错
  （已有 dispose 防护），归档失败仅记任务日志不崩
- Input 组件 {...rest} 透传 onKeyDown → PaperAsk 回车即问 ✓
- vlm-test 路由 readFileSync 同步读 30MB PDF（一次性诊断路由，C 级接受）

## 全流程测试（浏览器实测）

1. 设置页：研究方向预填 ✓、VLM 读图下拉（开/关）✓
2. 论文库：CCF-A/B 徽章 ✓；EdgeCrafter 详情：引用格式 GB/T→APA 切换 ✓
3. 报告预览：iframe 弹窗渲染 ✓
4. PaperAsk：EdgeCrafter（旧报告、无会话）正确显示引导文案 ✓；
   RT-RMOT 真实提问"RefRT 数据集规模"→ 25s 内返回带出处+置信度的回答 ✓
5. 从报告建卡：指令 274 字含 paper_read+绝对路径，发送命中（拦截）✓
6. 启动面板：默认单篇+学术深读+透镜勾选 ✓
7. read/run quick 直跑（新构建）：报告+1、五段式、summary 回填 ✓
8. 书柜分组视图 ✓、知识图谱 141 svg 元素 ✓
9. console 零错误；smoke 119 项全绿；13 工具注册 ✓

## 已知边界（记录不修）

- vlm-test 同步读大 PDF 阻塞事件循环（诊断用途，可接受）
- VLM 读图需多模态模型（glm-5.3 纯文本自动降级，v1.24 已测）
- read/run detached 无进度面板（jobs 绑定对话 agent，日志可查）
