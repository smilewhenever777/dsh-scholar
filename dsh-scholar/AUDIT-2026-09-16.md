# dsh-scholar 增量审计报告 · 2026-09-16

> **范围**：v1.8–v1.11 新增代码（readStatus 链路 / BibTeX / 分区侧栏+保存筛选 / 相关论文发现
> OpenAlex / 键盘导航+足迹+统计 / cites 引用同步），对照 2026-09-02 全量审计（AUDIT-2026-09-02.md）
> 之后的增量。**状态：6 项发现全部当日修复，typecheck+构建零错误，冒烟 106 项全绿，
> 3080 实例重启后浏览器回归零控制台错误。**

## 审计方法

- 通读 `related.ts`（全文）、`PaperLibraryView.tsx`（新增段落）、`GraphView.tsx`（cites 段）、
  `bibtex.ts`、`routes.ts`/`tools.ts`/`domain.ts`/`store.ts`/`index.ts`/`locales.ts` 增量
- 结构性检查：React hooks 声明顺序 vs 条件 return（已发生过 #300/TDZ 两次事故的两类坑）
- 交叉核对：saved-search 字段完整性 / 空态分支条件 / 工具参数校验对称性 / config 写入链路
- 上轮审计 P0-XSS/CSRF/守卫模式延续检查（guardRoute 覆盖新路由 ✓，JSON-only ✓，URL 编码 ✓）

## 发现与修复（6 项，无 P0）

| # | 级别 | 位置 | 问题 | 修复 |
|---|---|---|---|---|
| B1 | P1 | PaperLibraryView 键盘导航 | Enter 键在焦点停留于按钮（如刚点过的卡片）时双触发：window 处理器与按钮原生激活各调一次 openPaper，两篇论文竞态打开 | Enter 分支 `e.preventDefault()` 抑制原生激活，只走键盘游标 |
| B2 | P1 | 保存的筛选 | `SavedSearch.f` 漏存 `unfiled`：在"未分区"筛选下保存，应用时不还原（静默重置为 false） | f 增加 `unfiled: boolean`，存/应用两侧同步（旧数据无字段按 false） |
| B3 | P2 | 论文库空态 | `q || filtersActive` 未含 `col`：分区筛选 0 结果时误显示"库是空的+添加按钮"大空态（v1.2 起遗留，侧栏使其高频可见） | 条件补 `col`；"保存当前筛选"可用条件同步补齐 |
| B4 | P2 | tools.ts paper_search | `readStatus` 未校验枚举：模型传非法值（如 "read"）时静默返回 0 条，误导"库里没有" | `READ_STATUSES.includes()` 校验，非法视为未筛选 |
| B5 | P3 | railStats | 手改磁盘等途径写入非法 readStatus → `rs[未知]++` 得 NaN 计数（不崩但统计失真） | 枚举外兜底按 want 计数 |
| B6 | P3 | 详情页 venueBadge | 同一渲染内调用 `venueBadge(selected)` 3 次（含正则匹配），浪费且易漂移 | 收敛为 IIFE 内单次计算 |

## 复查确认无问题的关键点（抽查清单）

- hooks 顺序：全部 hook（含 yearGroups/flatPapers/cursorIdx/railStats）位于 `if (form)`/`if (selected)`
  early return 之前（line 673/698 之前的 1–672 区间）✓；GraphView `showCites` 声明先于 display memo ✓
- `/scholar/papers/:id/related`、`/scholar/graph/cite-sync` 均在 guardRoute 之后、方法校验齐全 ✓
- OpenAlex URL 构造全部 encodeURIComponent（title/doi），无注入面 ✓
- cite-sync 与 auto-sync 并发写图：Node 单线程内 compute→save 为同步段，无交错窗口 ✓
- BibTeX：中文作者+中文标题退路（fallback paper<id>）、重复键 -1/-2 消歧、转义顺序（\ 先于 %&#_{}）✓
- config 写入链路：`updateConfig = {...getConfig(), ...patch}` 全量合并，openalexEmail 不丢 ✓
- quickImportance 不带 readStatus —— PUT 部分更新语义下不误清（pickSubmitted 只保留提交字段）✓

## 复审增补（同日，应用户质询"为何已知不修"后）

原"已知不修"三条中两条的缓办理由不成立，已修：

| # | 原判 | 复审结论 | 修复 |
|---|---|---|---|
| B7（原①） | "仅 1 条边无影响" | 弱理由——该功能就是为边增长设计的，6 行可修 | GraphView display memo：隐藏 cites 边时同步隐藏"仅剩 cites 边"的节点（原本孤立者保留，Obsidian 过滤器语义） |
| B8（原③） | "跨页签重复可点，无害" | 审计描述不准：跨页签本就由共享 added map 正确标记；真残留是"入库后缓存期内重开详情再现入库按钮"。有干净修法 | related.ts：与库匹配标注从缓存对象中拆出，**改为每次请求现算**（缓存只存 OpenAlex 原始结果）。实测：入库后重挂载即标"已在库 ✓"（旧代码退回 0）；删除后即时回退 ＋入库（新增能力） |
| ② 维持 | 新预印本 OpenAlex refs=0 | 属数据源现实非代码缺陷；可选缓解为接入 Semantic Scholar 回退（新数据源集成，非 bug 修复），暂缓 | — |

增补验证：构建+冒烟 106 项全绿；实例重启后浏览器实测 B8 全链路（入库→重挂载→删除→回退）。

---

# 第二轮审计（v1.12–v1.13，同日追加）

> **范围**：Idea 卡片两批新增——看板/表格/证据字段/plain-steps 表单、卡片进图谱
> （idea 节点/cardSyncPatch/双同步/级联）、组合与实验桥、相似卡提醒、反链与搜索选择器。
> **状态：4 项发现（B9–B12）全部当日修复，冒烟 121 项全绿，浏览器回归通过。**

| # | 级别 | 位置 | 问题 | 修复 |
|---|---|---|---|---|
| B9 | P2 | CardForm steps 编辑 | **多行编辑坏**：受控值 `join('\n')` + onChange 即时 filter 空行，用户按回车的换行被立即吃掉，无法新增步骤行 | 拆出本地 `stepsText` 原文 state，textarea 绑原文，`save()` 时才 split/trim/filter（实测输入两行换行保留） |
| B10 | P2 | REST PUT /scholar/graph | mergeGraph 新增 idea 分支后，外部提交 `kind:'idea'` 的任意 id 节点可直通入库（幻影节点，"查看卡片"404）。kg_extract 通道无此问题（其归一化只产 paper/concept） | PUT 处理器过滤：`kind==='idea' && !store.cards.has(id)` 一律丢弃，响应透出 `droppedIdeaNodes` 计数（实测投幻影节点被拒） |
| B11 | P3 | GraphView idea 节点 | 菱形内嵌 8 字标签 + 侧边 20 字标签双份显示，菱形 ~9-13px 内文字溢出拥挤 | 移除内嵌文字，仅保留侧边标签（回归 27 菱形正常） |
| B12 | P3 | 表格视图 | 内层 `maxHeight: calc(100vh-220px)` 硬编码嵌套滚动（抽屉非全视口时高度错位，双滚动条） | 移除内层滚动，交外层 `.sch-scroll` 统一滚动 |

## 复查确认无问题的关键点

- **kg_extract 归一化**（tools.ts:802）：AI 提交的节点 kind 收敛为 paper/concept——幻影 idea 无法从对话通道进入 ✓
- **cardSyncPatch**：label 刷新走 mergeGraph 的同 id Map.set 覆盖；related 对端校验用"参数 ∪ 库内"（单卡 upsert 也能建边）；upsertCard 有空 patch 短路（不白写 graph.json）✓
- **看板 onDrop**：adopted/dropped 拖拽走 confirm（与按钮同级）；`{...c, status}` 全量 body 经 pickSubmitted 只留 schema 字段 ✓
- **saveCard/导出/桥 prompt**：依赖的 papers/allCards/CATEGORY_LABELS 均在作用域；剪贴板失败有降级 toast ✓
- **findSimilarCards**：O(n) token 集合运算，千卡量级无压力；excludeId/paperId 加分语义正确 ✓
- **hooks/声明顺序**：BookshelfView 无 early return，新增 state 安全；GraphView `d`→`ideaR` 顺序已修（TDZ 第三坑）✓

## 已知不修（记录在案，理由充分）

- 图谱 legend 现 9 种边——信息密度接近上限；若后续再加边类型应改为可折叠图例（当前不加边类型，暂不动）
- 表格视图列宽固定比例——27 列数据下良好；百卡级若"来源"列截断严重再引入列宽调整（新工程，非缺陷）

## 验证

- `npm run build`：typecheck(host)+tsdown+wrap 零错误（247317 bytes）
- `npm run smoke`：106 项全绿（含 readStatus×6、OpenAlex 解析×8、cites 边×2）
- 3080 实例重启后浏览器回归：论文库 21 卡/侧栏 10 项/统计条 ✓、搜索空词显示"无结果"小空态（B3）✓、
  ↑↓+Enter+Esc 键盘流（B1）✓、图谱页 cites 边渲染与同步按钮（TDZ 回归）✓、控制台 0 error 0 warning
